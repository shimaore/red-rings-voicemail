{
  "name": "red-rings-voicemail",
  "version": "1.1.3",
  "description": "red-rings CouchDB backend for voicemail settings and messages",
  "main": "index.js",
  "scripts": {
    "build": "npm install --only=dev && rm package-lock.json && coffee -c *.coffee.md",
    "prepublishOnly": "npm run build",
    "pretest": "npm run build && coffee -c test/*.coffee.md",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "github.com/shimaore/red-rings-voicemail-settings"
  },
  "author": "",
  "license": "Unlicense",
  "dependencies": {
    "abrasive-ducks-transducers": "^2.1.2",
    "ccnq4-kitty": "^1.0.2",
    "immutable": "^4.0.0-rc.9",
    "most": "^1.7.3",
    "red-rings": "^2.0.0",
    "red-rings-couchdb": "^3.5.1"
  },
  "devDependencies": {
    "coffeescript": "^2.2.3",
    "mocha": "^5.2.0"
  }
}
